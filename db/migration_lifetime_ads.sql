-- =====================================================================
-- Migration لهذه الجولة فقط: عدّاد "إجمالي الإعلانات المشاهدة مدى الحياة"
-- (يُستخدم لتفعيل "صديق نشط" عبر GigaPub/MonetixAds/بوابات الأزرار الست
-- بدل Adsgram، وأيضاً لفتح شخصية The Guardian الحصرية عند 4000 إعلان).
-- شغّلها مرة واحدة على D1 الحي (D1 Console أو wrangler).
-- =====================================================================

ALTER TABLE users ADD COLUMN lifetime_ads_watched INTEGER DEFAULT 0;
