-- =====================================================================
-- Migration لهذه الجولة فقط: تثبيت مهام Special/Partner من لوحة الأدمن.
-- شغّلها مرة واحدة على D1 الحي (D1 Console أو wrangler).
-- =====================================================================

-- pinned_at: وقت التثبيت نفسه بالمللي ثانية (NULL = غير مُثبَّتة). المهام
-- المُثبَّتة تظهر أولاً دائماً (للمستخدمين وفي لوحة الأدمن)، مرتَّبة بينها
-- بترتيب وقت التثبيت — الأقدم تثبيتاً يبقى أولاً، وأي مهمة تُثبَّت بعدها
-- تظهر بعدها مباشرة وليس قبلها.
ALTER TABLE admin_tasks ADD COLUMN pinned_at INTEGER;
